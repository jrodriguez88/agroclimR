% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_exp_aquacrop.R
\name{write_exp_aquacrop}
\alias{write_exp_aquacrop}
\title{Write AquaCrop project files}
\usage{
write_exp_aquacrop(
  path_proj,
  id_name,
  clim_name,
  soil_name,
  cultivar,
  sowing_date,
  harvest_date,
  co2_name = "MaunaLoa",
  irri_name = "rainfed",
  man_agro = "none",
  ini_cond = "none",
  version = "6.x"
)
}
\arguments{
\item{path_proj}{Character. AquaCrop project directory where the \code{LIST}
subdirectory and project file will be written.}

\item{id_name}{Character. Project identifier used as the \code{.PRM} file name.}

\item{clim_name}{Character. Base name of the AquaCrop climate file set,
without extension.}

\item{soil_name}{Character. Base name of the AquaCrop soil \code{.SOL} file,
without extension.}

\item{cultivar}{Character. Base name of the AquaCrop crop \code{.CRO} file,
without extension.}

\item{sowing_date}{Date vector. Sowing date or dates used to build simulation
runs.}

\item{harvest_date}{Date vector. Harvest date or dates used to build
simulation runs.}

\item{co2_name}{Character. Base name of the AquaCrop CO2 file, without
extension. Defaults to \code{"MaunaLoa"}.}

\item{irri_name}{Character. Irrigation file base name, or \code{"rainfed"} when no
irrigation file should be linked.}

\item{man_agro}{Character. Management file base name, or \code{"none"} when no
management file should be linked.}

\item{ini_cond}{Character. Initial-condition file base name, or \code{"none"} when
no initial-condition file should be linked.}

\item{version}{Character. AquaCrop version label used by the caller; defaults
to \code{"6.x"}.}
}
\value{
Character vector with the path of the AquaCrop project file created.
}
\description{
Creates AquaCrop project (\code{.PRM}) files that link climate, CO2, crop,
irrigation, management, soil and initial-condition files for one or more
simulation cycles. The function expects the referenced AquaCrop input files to
exist in the project directory.
}
\examples{
\dontrun{
prm_file <- write_exp_aquacrop(
  path_proj = tempdir(),
  id_name = "AIHU_FED2000_MADRI_S1",
  clim_name = "AIHU",
  soil_name = "AIHU",
  cultivar = "F2000",
  sowing_date = as.Date("2020-04-01"),
  harvest_date = as.Date("2020-08-15")
)
prm_file
}

}
