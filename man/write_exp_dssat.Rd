% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_exp_dssat.R
\name{write_exp_dssat}
\alias{write_exp_dssat}
\title{Write a DSSAT experimental file (X file)}
\usage{
write_exp_dssat(
  path,
  id_name,
  crop,
  cultivar,
  soil,
  wth_station,
  planting_details,
  irri,
  fert_in,
  start_date,
  planting_date,
  emergence_date,
  treatments_number
)
}
\arguments{
\item{path}{Character. Directory where the DSSAT experiment file will be
written.}

\item{id_name}{Character vector used to build the DSSAT experiment name and
output file name. The first element is the site or experiment identifier.}

\item{crop}{Character. DSSAT crop name, for example \code{"rice"}, \code{"maize"},
\code{"barley"}, \code{"sorghum"}, \code{"wheat"}, \code{"bean"}, \code{"fababean"}, or \code{"teff"}.}

\item{cultivar}{Character vector of DSSAT cultivar codes, one per treatment
when multiple treatments are written.}

\item{soil}{Character vector of DSSAT soil profile identifiers.}

\item{wth_station}{Character vector of DSSAT weather station identifiers.}

\item{planting_details}{List or data frame with DSSAT planting details such as
\code{PPOP}, \code{PPOE}, \code{PLME}, \code{PLDS}, \code{PLRS}, \code{PLRD}, and \code{PLDP}.}

\item{irri}{Logical. \code{TRUE} for irrigated experiments and \code{FALSE} for rainfed
experiments.}

\item{fert_in}{Data frame or \code{NULL}. Fertilization schedule with DSSAT fields
such as \code{FDATE}, \code{FMCD}, \code{FACD}, \code{FDEP}, \code{FAMN}, \code{FAMP}, \code{FAMK}, \code{FAMC},
\code{FAMO}, \code{FOCD}, and \code{FERNAME}.}

\item{start_date}{Date. Simulation start date.}

\item{planting_date}{Date vector. Planting date for each treatment.}

\item{emergence_date}{Date vector or \code{NULL}. Emergence date for each
treatment.}

\item{treatments_number}{Integer. Number of DSSAT treatments to write.}
}
\value{
Character vector with the path of the DSSAT experiment file created.
}
\description{
Creates a DSSAT experiment file from already prepared experiment metadata,
planting information, soil/weather links, irrigation flags and fertilization
inputs. This is the low-level writer used after experiment data have been
tidied into one row per DSSAT treatment setup.
}
\details{
Use \code{tidy_exp_dssat()} to prepare the row-wise inputs consumed by
this writer.
}
\examples{
\dontrun{
# Experimental tidy data
tidy_exp_data <- tidy_exp_dssat(agro, phenol, fertil, path = tempdir())

# Write DSSAT experimental files
exp_files_created <- tidy_exp_data \%>\%
  mutate(file = pmap_chr(., write_exp_dssat)) \%>\%
  pull(file)

exp_files_created
file.remove(exp_files_created)
}

}
